<%= turbo_stream.replace "form" do %>
  <%= render partial: "comments/form", locals: { comment: @comment, post: @post } %>
<% end %>

<%= turbo_stream.append "comments-of-#{@post.id}" do %>
 <%= render @comment %>
<% end %>
