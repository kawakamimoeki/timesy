<div class="shadow-sm mb-4 rounded-lg relative bg-white p-4 transition-opacity animate-fade-in">
  <div class="flex items-start">
    <%= render partial: "shared/avatar", locals: { user: item } %>
    <div class="grow"></div>
    <%= render partial: "follows/button", locals: { user: item } %>
  </div>
  <div class="ml-10">
    <%= item.bio %>
  </div>
</div>
