<% set_meta_tags(
  title: "New Project",
  og: {
    title: "New Project",
  }
) %>

<div class="grow max-w-[calc(100vw-2rem)] md:max-w-[calc(100%-256px-1rem)] p-4">
  <%= render partial: 'projects/form', locals: { project: @project, path: create_project_path, submit_label: I18n.t("form.submit.create"), method: :post } %>
</div>
