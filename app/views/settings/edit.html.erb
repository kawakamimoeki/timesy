<% set_meta_tags(
  title: "Setting",
  og: {
    title: "Setting",
  }
) %>

<%= turbo_frame_tag "timeline" do %>
<div class="max-w-3xl w-full mx-auto p-0 sm:p-4">
  <%= render partial: "users/settings", locals: { user: @user } %>
</div>
<% end %>
