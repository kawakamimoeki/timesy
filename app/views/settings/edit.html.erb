<% set_meta_tags(
  title: "Setting",
  og: {
    title: "Setting",
  }
) %>

<div class="grow h-[calc(100vh-55px)] overflow-scroll">
  <%= render partial: "users/settings", locals: { user: @user } %>
</div>
